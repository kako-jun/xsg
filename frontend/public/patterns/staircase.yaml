# Staircase Pattern
# 21-step grayscale gradient (0% to 100% in 5% steps)

extends: gradient.yaml

# Override parameters
params:
  steps:
    type: number
    default: 21
  direction:
    type: string
    default: "horizontal"
