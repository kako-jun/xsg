# Horizontal Gradient Pattern
# 16-step horizontal grayscale gradient (black to white)

extends: gradient.yaml

# Override parameters
params:
  steps:
    type: number
    default: 16
  direction:
    type: string
    default: "horizontal"
