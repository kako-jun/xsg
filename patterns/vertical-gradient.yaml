# Vertical Gradient Pattern
# 16-step vertical grayscale gradient (black to white)

name: "Vertical Gradient"
category: "Grayscale & Gradients"

extends: gradient.yaml

# Override parameters
params:
  steps:
    type: number
    default: 16
  direction:
    type: string
    default: "vertical"
